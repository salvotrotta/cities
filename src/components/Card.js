import React from 'react';
import { View } from 'react-native';

const Card = () => {
  return (
    <View style={styles.cardStyle}>
    </View>
  );
};

const Styles = {
  cardStyle: {
    borderWidth: 1,
    borderRadius: 2,
    borderColor: '#DDD',
    borderBottomWidth: 0
  }
};

export default Card;
